const dbIndex = require('../models/index');
let Travel = dbIndex.db.Travel;
const util = require('../_helpers/util');
const config = require('../config');

async function getTravel(req, res){

}

module.exports.getTravel = getTravel;

